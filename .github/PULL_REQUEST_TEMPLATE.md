## Summary

## Motivation

## Tests

Commands run and their results (`npm run typecheck`, `npm test`, `npm run build`):

## Security impact

Does this change auth, token/credential handling, local state permissions,
or Supabase access? If so, describe it. If not, say "None".

## Breaking changes

## Checklist

- [ ] `npm run typecheck`, `npm test`, and `npm run build` pass locally
- [ ] Tests added or updated for the behavior that changed
- [ ] No secrets, tokens, or real user data in the diff
- [ ] Docs updated if behavior or setup changed
